// Create a rectangular shadow caster without the need for a sprite. You give the 
// half width and half height and the shadow will be generated around the x/y position
// of the instance calling the script.

Aura_Shadow_Caster_Rect_Init(random(32), random(32));